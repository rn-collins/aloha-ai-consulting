const {catalog,enabled}=require('../_lib/commerce');
module.exports=async function(req,res){res.setHeader('Cache-Control','no-store');if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({ok:false,error:'method_not_allowed'});}const c=catalog();const config=['STRIPE_SECRET_KEY','STRIPE_WEBHOOK_SECRET','SUPABASE_URL','SUPABASE_SERVICE_ROLE_KEY','BLOB_READ_WRITE_TOKEN','COMMERCE_SIGNING_SECRET','RESEND_API_KEY','COMMERCE_FROM_EMAIL','COMMERCE_SUPPORT_EMAIL','COMMERCE_SITE_URL'].map(name=>({name,present:Boolean(process.env[name])}));return res.status(enabled()?200:503).json({ok:enabled(),state:enabled()?'open':'closed',configuration:config,globalBlockers:c.globalBlockers,productStates:c.products.map(p=>({resourceId:p.resourceId,state:p.state,blockerCount:p.blockers.length})),workspaceEntitlement:false});};
