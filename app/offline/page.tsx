import type {Metadata} from "next";import Link from "next/link";import {SiteShell} from "../site-shell";import {ReconnectButton} from "./reconnect-button";
export const metadata:Metadata={title:"Offline",description:"A clear recovery route when Aloha AI cannot reach the network."};
export default function Offline(){return <SiteShell><main id="main"><section className="page-hero wrap"><p className="kicker">Offline · saved shell</p><h1>You are offline—<em>your browser-local work may still be here.</em></h1><p className="lede">Reconnect to open uncached pages or current evidence. Do not clear site data if you need a locally saved course or decision record.</p><div className="actions"><ReconnectButton/><Link className="button" href="/support/">Recovery guidance</Link></div></section><section className="boundary wrap"><p className="section-label">Recovery boundary</p><div><h2>Offline access is deliberately limited.</h2><p>The saved public shell is not proof that evidence, policies, or course materials are current. Export important browser-local records when you reconnect.</p></div></section></main></SiteShell>}
