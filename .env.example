# This static public repository is expected to run without secrets.
# Add variable names here only when a server-side or build-time integration is introduced.
# Never add real credentials, tokens, account identifiers, personal data, or production URLs containing secrets.
